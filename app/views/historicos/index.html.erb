<h1 class="text-center mt-4">Disciplinas já cursadas:</h1>

<div class="apple_pagination">
<div class="page_info">
    <%= page_entries_info @historicos %>
</div>
    <%= will_paginate @historicos, :container => false %>
</div>
<div class="container">
  <% @historicos.each do |historico| %>
    <div class="row justify-content-md-center">
      <div class="col-8 mt-4">
        <div class="card text-center shadow mb-5 bg-white rounded">
          <div class="card-header font-italic">
             <%= historico.user.nome %>
          </div>
          <div class="card-body">
            <h5 class="card-title" ><%= historico.disciplina.nome %></h5>
            <p class="card-text"><%= historico.nota %></p>
            <p class="card-text"><%= historico.aprovado ? "Aprovado" : "Reprovado" %></p>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>
<div class="apple_pagination">
    <%= will_paginate @historicos, :container => false %>
</div>